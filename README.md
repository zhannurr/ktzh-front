
  # Create prompt (Copy)

  This is a code bundle for Create prompt (Copy). The original project is available at https://www.figma.com/design/IWvA8sNyLzLPiSaEfv8WVl/Create-prompt--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  